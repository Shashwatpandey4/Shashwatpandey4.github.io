# figs/

Figure generators for the blog posts. Output is inline SVG committed into the
markdown — no image assets, no plotting runtime on the page.

## Two styles, on purpose

- **Crisp SVG**, hand-written in the post, for anything **quantitative**: the
  ladder chart, the A6000 comparison, bandwidth bars. Precise lengths matter when
  a reader is comparing magnitudes, and hand-drawn wobble actively interferes.
- **Hand-drawn (`rough.cjs`)**, generated by script, for anything
  **conceptual**: memory hierarchies, access patterns, the outer product,
  pipeline timelines. These are schematics, not measurements, and looking drawn
  rather than plotted tells the reader which one they're looking at.

`rough.cjs` wraps [roughjs](https://roughjs.com), the library Excalidraw uses,
via its headless generator — so the Excalidraw aesthetic is reproducible from a
script with no GUI export step.

## Palette

Blue `#1a73e8`, orange `#c2410c`, neutral `#6b7280`.

Validated with the six-check validator: lightness band PASS, CVD separation
PASS (worst adjacent pair ΔE 14.3 deutan, target ≥ 8), normal-vision floor PASS
(ΔE 19.1), contrast PASS. The neutral trips the chroma floor by design — it is
meant to read as gray, not as a series.

**Green is deliberately absent.** The original palette paired `#15803d` with
`#c2410c` at ΔE 7.0 under deuteranopia — the classic red/green trap — and in
these figures those two colors carry "fast" and "slow". Blue/orange is the
CVD-safe pair for that job.

## Two rules the markdown pipeline imposes

Both of these broke every figure in Part 1 once, so they are not optional:

1. **No blank lines inside `<figure>`.** In CommonMark an HTML block ends at the
   first blank line; marked then parses the rest of the figure as markdown.
2. **Indent at most two spaces.** Four or more starts an indented code block, so
   the tail of the figure renders as visible escaped markup.

`build_prototypes.cjs` asserts both after writing. Always run the output through
the same marked build the site loads before pushing — validating the SVG as
standalone XML is not the same test.

## Usage

```bash
cd figs && npm install roughjs && node build_prototypes.cjs
```
